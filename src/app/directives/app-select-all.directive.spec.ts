import { AppSelectAllDirective } from './app-select-all.directive';

describe('AppSelectAllDirective', () => {
  it('should create an instance', () => {
    const directive = new AppSelectAllDirective(undefined);
    expect(directive).toBeTruthy();
  });
});
