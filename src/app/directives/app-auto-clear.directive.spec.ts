import { AppAutoClearDirective } from './app-auto-clear.directive';

describe('AppAutoClearDirective', () => {
  it('should create an instance', () => {
    const directive = new AppAutoClearDirective(undefined);
    expect(directive).toBeTruthy();
  });
});
