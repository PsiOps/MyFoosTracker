import { PlayerSearchPipe } from './player-search.pipe';

describe('PlayerSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
